# =============================================================
# Docker Watch (.env) — /opt/docker-watch/.env
# =============================================================

# --- Telegram (obligatorio) ---
BOT_TOKEN="123456789:AAExampleTokenReplaceMe"
CHAT_ID="-1001234567890"

# --- Filtrado por label (recomendado para evitar ruido) ---
# Monitorea SOLO contenedores con este label:
MONITOR_LABEL_KEY="dockwatch.monitor"
MONITOR_LABEL_VALUE="true"

# --- Ruido / verbosidad ---
NOTIFY_START="false"      # true => notifica start (puede ser ruidoso)
NOTIFY_HEALTHY="false"    # true => notifica cuando vuelve a healthy

# --- Anti-spam (TTL por evento y contenedor) ---
TTL_DIE_SEC="300"
TTL_UNHEALTHY_SEC="600"
TTL_START_SEC="600"

# --- Auto-restart controlado (opcional) ---
AUTO_RESTART_ON_DIE="false"
RESTART_COOLDOWN_SEC="120"
RESTART_MAX_PER_HOUR="3"

# (Opcional) Auto-restart SOLO si el contenedor tiene este label:
# AUTO_RESTART_LABEL_KEY="dockwatch.autorestart"
# AUTO_RESTART_LABEL_VALUE="true"
