#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$DIR"
mkdir -p dist
bash scripts/server/package-server.sh
bash scripts/web/package-web.sh
printf 'Packages generated under %s/dist\n' "$DIR"
